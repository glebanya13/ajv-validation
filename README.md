# 🛡 AJV Validation
This project is a Vue.js application that provides a simple login form with email and password fields, along with form validation using a JSON schema. The application is built using the Vue CLI and utilizes the `ajv` library for JSON schema validation. The project's core features include a login form with validation, error handling, and a clean, responsive design.

## 🚀 Features
* Login form with email and password fields
* Form validation using a JSON schema
* Error handling and display
* Responsive design
* Clean and minimalistic UI

## 🛠️ Tech Stack
* **Frontend:** Vue.js, Vue CLI
* **Validation:** `ajv` library, `ajv-formats` for format validation, `ajv-errors` for error handling
* **Build Tools:** `@vue/cli-service`
* **Dependencies:** `vue`, `ajv`, `ajv-formats`, `ajv-errors`

## 📦 Installation
To get started with the project, follow these steps:
1. Clone the repository using `git clone`
2. Install the dependencies using `npm install` or `yarn install`
3. Start the development server using `npm run serve` or `yarn serve`

## 💻 Usage
1. Open the application in your web browser at `http://localhost:8080`
2. Fill in the login form with your email and password
3. Submit the form to see the validation in action

## 📂 Project Structure
```markdown
.
├── public
├── src
│   ├── main.ts
│   ├── App.vue
│   ├── schemas
│   │   ├── login.json
│   └── ...
├── vue.config.js
├── package.json
└── ...
```

## 🤝 Contributing
Contributions are welcome! If you'd like to contribute to the project, please fork the repository and submit a pull request with your changes.

## 📝 License
This project is licensed under the MIT License.

## 📬 Contact
For any questions or concerns, please don't hesitate to reach out to us.
